[package]
name = "lastz_auto_agent"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
libc = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
